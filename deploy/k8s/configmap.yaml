apiVersion: v1
kind: ConfigMap
metadata:
  name: rclob-config
  namespace: rclob
data:
  PORT: "8080"
  REDIS_ADDR: "redis://redis-client:6379/0"
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
  REDIS_MAX_RETRIES: "3"
